{
  "name":        "vitest-leak-assert",
  "version":     "0.1.0",
  "description": "Vitest reporter + helper for leak-assert",
  "main":        "dist/index.js",
  "types":       "dist/index.d.ts",
  "keywords":    ["vitest", "memory", "leak"],
  "license":     "MIT",
  "scripts": { "build": "tsc" },
  "peerDependencies": {
    "vitest":                        ">=1.0",
    "@anandsrinivasan2/leak-assert": ">=0.1"
  },
  "devDependencies": {
    "typescript":  "^5.0.0",
    "@types/node": "^20.0.0",
    "vitest":      "^1.0.0"
  }
}
